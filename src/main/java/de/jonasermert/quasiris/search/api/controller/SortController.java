package de.jonasermert.quasiris.search.api.controller;


/*
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;

import java.util.Comparator;

@Controller("SortController")
public class SortController {


    @GetMapping("/sort")
    public String sort(@RequestParam(name = "sortOption", defaultValue = "name") String sortOption, Model model) {
        List<MyObject> objects =

        /
        if ("date".equals(sortOption)) {
            objects.sort(Comparator.comparing(MyObject::getDate));
        } else if ("size".equals(sortOption)) {
            objects.sort(Comparator.comparing(MyObject::getSize));
        } else {
            objects.sort(Comparator.comparing(MyObject::getName));
        }

        model.addAttribute("objects", objects);
        return "index";
    }

}

*/
